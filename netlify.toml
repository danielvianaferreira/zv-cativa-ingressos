[build]
  publish = "."
  command = ""
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# Redireciona /api/jogos → função serverless
[[redirects]]
  from = "/api/jogos"
  to   = "/.netlify/functions/jogos"
  status = 200
  force = true
